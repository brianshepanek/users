- service: app
  type: push
  name: build-push
  image_name: brianshepanek/users-golang
  image_tag: "{{.CI_STRING_TIME}}"
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted